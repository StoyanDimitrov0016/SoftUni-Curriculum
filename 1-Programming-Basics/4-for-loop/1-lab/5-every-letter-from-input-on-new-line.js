function charSequence(input) {
    let inputText = input[0];

    for (let i = 0; i < inputText.length; i++) {
        let letter = inputText[i];
        console.log(letter);
    }
}
charSequence(["Stoyan Dimitrov"]);